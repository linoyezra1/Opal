# Vite dev: point API calls to the Node server (Vite default is http://localhost:5173)
VITE_API_URL=http://localhost:3001

# Optional: slug of a Landing Page (built in /admin/landing-pages) — when set, "/" shows that dynamic page
# VITE_PUBLIC_HOME_LANDING_SLUG=my-org-landing
